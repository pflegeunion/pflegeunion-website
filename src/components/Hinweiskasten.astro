---
/**
 * Baustein C – Hinweiskasten «Gut zu wissen» (Konzept V3 B4).
 *
 * Fläche tiefblau-hell, Etikette in Ocker, Titel Lato Bold, Inhalt als Slot.
 * Für Definitionen und ehrliche Grenzen. Mehrfach pro Seite erlaubt.
 *
 * Parameter:
 *   etikette  Übertitel (Standard «GUT ZU WISSEN»)
 *   titel     Titel des Kastens
 */
interface Props {
  etikette?: string;
  titel: string;
}

const { etikette = 'GUT ZU WISSEN', titel } = Astro.props;
---

<div class="kasten">
  <p class="text-label etikette">{etikette}</p>
  <h3 class="text-h3">{titel}</h3>
  <div class="kasten-inhalt">
    <slot />
  </div>
</div>

<style>
  .kasten {
    background-color: var(--color-tiefblau-hell);
    padding: var(--space-3);
  }

  .kasten h3 {
    color: var(--color-schwarz);
  }

  @media (min-width: 768px) {
    .kasten {
      padding: var(--space-4);
    }
  }
</style>
